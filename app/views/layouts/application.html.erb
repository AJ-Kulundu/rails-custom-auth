<!DOCTYPE html>
<html>
  <head>
    <title>Auth</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="nav">
      <%if user_signed_in?%>
        <%= button_to "Logout",sessions_path,method: :delete, class: "nav-item",data: {turbo_confirm: "Are you sure you want to logout?" }%>
      <%else%>
        <%= link_to "Login",new_sessions_path, class: "nav-item"%>
        <%= link_to "Sign up",new_users_path, class: "nav-item"%>
      <%end%>
    </header>
    <div><%=notice%></div>
    <div><%=alert%></div>
    <%= yield %>
  </body>
</html>
